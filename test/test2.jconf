# am using two spaces for indentation here

__nonstrictindent__

lang
  python
    keywords[]
      if
      
       # this item is not well indented but since __nonstrictindent__ as set, it will parse ok
       else
      
      is
      and

    # for my bool, am using yes/no instead of true/false
    compiled:b = no

  C
    keywords[]
      enum
      typedef
      return
    
    compiled:b = yes